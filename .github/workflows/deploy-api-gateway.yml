name: Deploy api-gateway

on:
  push:
  workflow_dispatch:

jobs:
  deploy:
    permissions:
      contents: read
      id-token: write
    uses: ./.github/workflows/service-deploy.yml
    secrets: inherit
    with:
      service_name: api-gateway
      service_dir: .
